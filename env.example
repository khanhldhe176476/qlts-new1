# Database Configuration
# Sử dụng PostgreSQL của Docker (đồng bộ với Docker)
DATABASE_URL=postgresql://qlts_user:qlts_password@localhost:5433/qlts_db
# Hoặc dùng SQLite local (uncomment dòng dưới và comment dòng trên):
# DATABASE_URL=sqlite:///./instance/app.db
SECRET_KEY=dev-key-change-in-production
ADMIN_USERNAME=admin
ADMIN_PASSWORD=mh123#@!
ADMIN_EMAIL=admin@example.com

# Flask Configuration
FLASK_ENV=development
FLASK_DEBUG=True

# Export Directory (where server saves a copy of exported files)
# Use absolute path or keep default which resolves to <project>/instance/exports
# EXPORT_DIR=instance/exports

# For Production (uncomment and configure)
# DATABASE_URL=postgresql://username:password@localhost/asset_management
# SECRET_KEY=your-production-secret-key
# FLASK_ENV=production
# FLASK_DEBUG=False

# Email Configuration (for asset transfer confirmation and notifications)
# Ví dụ cấu hình Yandex cho noreply@mhsolution.vn
EMAIL_ENABLED=true
MAIL_SERVER=smtp.yandex.com
MAIL_PORT=465
MAIL_USE_TLS=false
MAIL_USE_SSL=true
MAIL_USERNAME=noreply@mhsolution.vn
MAIL_PASSWORD=alfbxvshroqjkwnr
MAIL_DEFAULT_SENDER=noreply@mhsolution.vn
APP_URL=http://localhost:5000

# Email Validation API (Optional - for advanced email validation)
# EMAIL_VALIDATION_API_KEY=your_zerobounce_api_key
# EMAIL_VALIDATION_PROVIDER=zerobounce